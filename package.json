{
  "name": "development-pattern-for-web-apps",
  "version": "1.0.0",
  "private": true,
  "description": "Infraestrutura mínima para recompilar o CSS da página do projeto (docs/). A skill em si não tem build.",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./docs/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./tailwind.input.css -o ./docs/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
